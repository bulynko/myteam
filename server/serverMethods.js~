Meteor.methods({

    //=========================================================
    removeUser: function (id) {
    console.log( " remove id:" + id);
    users.remove({_id: id}); 
  }
  
  
  })
  
  
